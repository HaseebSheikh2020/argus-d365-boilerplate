import * as React from "react";
import * as ReactDOM from "react-dom";

const PlayGroundApp = () => {
  return <h1>Playground App2</h1>;
};

ReactDOM.render(<PlayGroundApp />, document.getElementById("root"));
